import React from "react";
import ReactDOM from "react-dom/client";
import AllProvider from "AllProvider.tsx";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <AllProvider />
  </React.StrictMode>
);
